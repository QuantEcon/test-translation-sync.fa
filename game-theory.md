---
jupytext:
  text_representation:
    extension: .md
    format_name: myst
kernelspec:
  display_name: Python 3
  language: python
  name: python3
translation:
  title: مبانی نظریه بازی‌ها
  headings:
    Introduction to Strategic Thinking: مقدمه‌ای بر تفکر راهبردی
    The Prisoner's Dilemma: معمای زندانی
    The Prisoner's Dilemma::Real-World Applications: کاربردهای دنیای واقعی
    Nash Equilibrium: تعادل نش
    Nash Equilibrium::Mixed Strategies: استراتژی‌های آمیخته
    Sequential Games: بازی‌های ترتیبی
    Sequential Games::Stackelberg Competition: رقابت استکلبرگ
    Repeated Games: بازی‌های تکرارشونده
    Repeated Games::Trigger Strategies: استراتژی‌های ماشه‌ای
    Exercises: تمرینات
---

# مبانی نظریه بازی‌ها

این درس مفاهیم بنیادین نظریه بازی‌ها و کاربردهای آن‌ها را در تصمیم‌گیری اقتصادی معرفی می‌کند. ما تعاملات راهبردی، تعادل نش و ساختارهای رایج بازی را بررسی خواهیم کرد.

## مقدمه‌ای بر تفکر راهبردی

نظریه بازی‌ها چارچوبی برای تحلیل موقعیت‌هایی فراهم می‌کند که در آن‌ها چند تصمیم‌گیرنده به‌صورت راهبردی با یکدیگر تعامل دارند. انتخاب بهینه هر بازیکن به انتخاب دیگران بستگی دارد و این امر وابستگی‌های متقابل پیچیده‌ای ایجاد می‌کند.

در بازارهای اقتصادی، بنگاه‌هایی که بر سر قیمت یا مقدار رقابت می‌کنند، درگیر بازی‌های راهبردی می‌شوند. درک این تعاملات به پیش‌بینی نتایج بازار و طراحی سازوکارهای بهتر کمک می‌کند.

## معمای زندانی

معمای زندانی کلاسیک نشان می‌دهد که چرا افراد عقلایی ممکن است حتی زمانی که همکاری به نفع همه است، همکاری نکنند. دو مظنون به‌طور جداگانه بازجویی می‌شوند و باید تصمیم بگیرند که اعتراف کنند یا سکوت کنند.

ماتریس بازده ساختار راهبردی را نشان می‌دهد:

|                | همکاری    | خیانت      |
|----------------|-------------|-------------|
| **همکاری**  | (-1, -1)    | (-3, 0)     |
| **خیانت**     | (0, -3)     | (-2, -2)    |

هر بازیکن یک استراتژی غالب برای خیانت دارد که به نتیجه‌ای نامطلوب برای هر دو منجر می‌شود.

### کاربردهای دنیای واقعی

ساختار معمای زندانی در بسیاری از زمینه‌های اقتصادی دیده می‌شود:
- رقابت قیمتی میان بنگاه‌ها
- عرضه کالاهای عمومی
- توافقات حفاظت از محیط زیست
- مسابقه تسلیحاتی و هزینه‌های نظامی

درک این بازی به توضیح شکست‌های بازار و نیاز به تنظیم‌گری یا سازوکارهای همکاری کمک می‌کند.

## تعادل نش

تعادل نش یک نمایه استراتژی است که در آن هیچ بازیکنی نمی‌تواند با تغییر یک‌جانبه استراتژی خود، بازده خود را بهبود بخشد. این تعادل نشان‌دهنده یک نتیجه پایدار است که در آن استراتژی هر بازیکن با توجه به استراتژی‌های دیگران بهینه است.

به‌صورت ریاضی، برای بازیکنان $i = 1, \ldots, n$ با مجموعه‌های استراتژی $S_i$ و توابع بازده $u_i$، یک نمایه استراتژی $(s_1^*, \ldots, s_n^*)$ تعادل نش است اگر:

$$
u_i(s_i^*, s_{-i}^*) \geq u_i(s_i, s_{-i}^*) \quad \forall s_i \in S_i, \forall i
$$

بیایید تعادل‌های نش را برای یک بازی ساده در Python محاسبه کنیم:

```{code-cell} python
import numpy as np
import nashpy as nash

# Define a simple 2x2 game
# Player 1's payoff matrix
A = np.array([[3, 0],
              [5, 1]])

# Player 2's payoff matrix  
B = np.array([[3, 5],
              [0, 1]])

# Create game
game = nash.Game(A, B)

# Find Nash equilibria
equilibria = list(game.support_enumeration())

print("Nash Equilibria:")
for eq in equilibria:
    print(f"Player 1: {eq[0]}, Player 2: {eq[1]}")
    print(f"Payoffs: ({np.dot(eq[0], A @ eq[1])}, {np.dot(eq[0], B.T @ eq[1])})")
    print()
```

### استراتژی‌های آمیخته

هنگامی که هیچ تعادل نش با استراتژی خالص وجود نداشته باشد، بازیکنان ممکن است بین استراتژی‌ها به‌صورت تصادفی انتخاب کنند. این تعادل‌های استراتژی آمیخته نیازمند شرایط بی‌تفاوتی هستند که در آن‌ها بازیکنان مایل به انتخاب تصادفی می‌باشند.

احتمالات تعادل استراتژی آمیخته را می‌توان با حل معادلات زیر محاسبه کرد:

```{math}
\begin{align}
p \cdot u_1(\text{Strategy 1}) &= p \cdot u_1(\text{Strategy 2}) \\
q \cdot u_2(\text{Strategy 1}) &= q \cdot u_2(\text{Strategy 2})
\end{align}
```

## بازی‌های ترتیبی

در بازی‌های ترتیبی، بازیکنان به ترتیب مشخصی حرکت می‌کنند و اقدامات پیشین را مشاهده می‌کنند. این بازی‌ها با استفاده از استقرای پس‌رونده تحلیل می‌شوند که از انتهای درخت بازی به سمت ابتدا پیش می‌رود.

بینش کلیدی، کامل بودن زیربازی است: در هر گره تصمیم، بازیکنان با توجه به بازی آینده به‌صورت بهینه انتخاب می‌کنند. این امر تهدیدهای غیرقابل‌باور را که بازیکنان عقلایی هرگز اجرا نمی‌کنند حذف می‌کند.

### رقابت استکلبرگ

یک کاربرد کلاسیک، رقابت استکلبرگ است که در آن یک بنگاه (پیشرو) ابتدا مقدار را انتخاب می‌کند و بنگاه دیگر (پیرو) این انتخاب را مشاهده می‌کند و سپس تصمیم می‌گیرد.

پیشرو تابع واکنش پیرو را پیش‌بینی می‌کند و به‌صورت بهینه انتخاب می‌کند:

$$
\max_{q_L} \pi_L(q_L, R(q_L))
$$

که در آن $R(q_L)$ بهترین پاسخ پیرو به مقدار پیشرو $q_L$ است.

بیایید یک بازی استکلبرگ را به‌صورت عددی حل کنیم:

```{code-cell} python
from scipy.optimize import minimize_scalar

# Market parameters
a = 100  # Demand intercept
c = 10   # Marginal cost

# Follower's reaction function: q_F = (a - c - q_L) / 2
def follower_response(q_L):
    return (a - c - q_L) / 2

# Leader's profit given follower's response
def leader_profit(q_L):
    q_F = follower_response(q_L)
    price = a - q_L - q_F
    return -(price - c) * q_L  # Negative for minimization

# Find leader's optimal quantity
result = minimize_scalar(leader_profit, bounds=(0, a-c), method='bounded')
q_L_optimal = result.x
q_F_optimal = follower_response(q_L_optimal)

print(f"Leader quantity: {q_L_optimal:.2f}")
print(f"Follower quantity: {q_F_optimal:.2f}")
print(f"Market price: {a - q_L_optimal - q_F_optimal:.2f}")
print(f"Leader profit: {-result.fun:.2f}")
```

## بازی‌های تکرارشونده

هنگامی که بازی‌ها به‌طور تکراری بازی می‌شوند، تعادل‌های جدیدی از طریق سازوکارهای شهرت و مجازات پدید می‌آیند. قضیه فولک نشان می‌دهد که وقتی بازیکنان به اندازه کافی صبور باشند، بسیاری از نتایج می‌توانند به‌عنوان تعادل حفظ شوند.

سازوکار کلیدی، تهدید به بازگشت به بازی غیرهمکارانه در صورت انحراف هرکس از مسیر همکاری است. این امر مستلزم موارد زیر است:

۱. بازیکنان برای بازده‌های آینده ارزش قائل باشند (ضریب تنزیل $\delta < 1$)
۲. انحرافات قابل مشاهده باشند
۳. تهدیدهای مجازات قابل‌باور باشند

### استراتژی‌های ماشه‌ای

یک سازوکار اجرایی رایج، استراتژی ماشه سنگین است: همکاری تا زمانی که کسی خیانت کند و سپس خیانت برای همیشه. این استراتژی می‌تواند همکاری را در شرایط زیر حفظ کند:

$$
\frac{1}{1-\delta} \cdot \pi_{\text{coop}} \geq \pi_{\text{deviate}} + \frac{\delta}{1-\delta} \cdot \pi_{\text{punish}}
$$

سمت چپ، بازده حاصل از همکاری دائمی است، در حالی که سمت راست سود حاصل از انحراف و پس از آن مجازات دائمی است.

## تمرینات

۱. **محاسبه تعادل‌ها**: تمام تعادل‌های نش (خالص و آمیخته) را برای بازی زیر بیابید:

   |           | چپ  | راست |
   |-----------|-------|-------|
   | **بالا**    | (3,2) | (1,3) |
   | **پایین**  | (0,1) | (2,4) |

۲. **بازی‌های تکرارشونده**: حداقل ضریب تنزیل لازم برای حفظ همکاری در یک معمای زندانی تکرارشونده نامتناهی با استفاده از استراتژی‌های ماشه سنگین را محاسبه کنید.

۳. **بازی‌های ترتیبی**: تعادل کامل زیربازی را در یک بازی ورود سه‌مرحله‌ای که در آن بنگاه‌ها به‌ترتیب تصمیم می‌گیرند وارد بازار شوند یا نه، حل کنید.

۴. **استراتژی‌های آمیخته**: تأیید کنید که تعادل استراتژی آمیخته‌ای که برای تمرین ۱ محاسبه کرده‌اید، هر دو بازیکن را نسبت به استراتژی‌های خالص خود بی‌تفاوت می‌سازد.